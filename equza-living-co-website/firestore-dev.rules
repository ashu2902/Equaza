rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT MODE - ALLOW ALL READS (TEMPORARY)
    match /{document=**} {
      allow read: if true; // Allow all reads in development
      allow write: if false; // Still prevent writes for safety
    }
  }
}